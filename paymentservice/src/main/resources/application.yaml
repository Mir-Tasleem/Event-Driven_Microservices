spring:
  main:
    web:
      application-type: none
  application:
    name: paymentservice

  datasource:
    url: jdbc:postgresql://pg-payment:5432/payment_db
    username: payment_user
    password: payment_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  management:
    endpoints:
      web:
        exposure:
          include: health,info
    endpoint:
      health:
        show-details: always

logging:
  level:
    root: INFO
