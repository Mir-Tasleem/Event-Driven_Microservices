apiVersion: v1
kind: Secret
metadata:
  name: pg-inventory-secret
  namespace: ecomerce
stringData:
  POSTGRES_USER: inventory_user
  POSTGRES_PASSWORD: inventory_pass
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-inventory-pvc
  namespace: ecomerce
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pg-inventory
  namespace: ecomerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pg-inventory
  template:
    metadata:
      labels:
        app: pg-inventory
    spec:
      containers:
        - name: pg-inventory
          image: postgres:15-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: pg-inventory-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-inventory-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: pg-inventory-pvc
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command: ["pg_isready","-U","inventory_user"]
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: pg-inventory-pvc
          persistentVolumeClaim:
            claimName: pg-inventory-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: pg-inventory
  namespace: ecomerce
spec:
  selector:
    app: pg-inventory
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5434
      targetPort: 5432
